NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Auth (magic-link email)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
DYNAMODB_TABLE_NAME=
ANTHROPIC_API_KEY=
# Optional — defaults to claude-sonnet-4-5
ANTHROPIC_MODEL=
# exchangerate.host access key. Optional — without it the FX service falls
# back to the keyless open.er-api.com provider (daily rates).
FX_API_KEY=
